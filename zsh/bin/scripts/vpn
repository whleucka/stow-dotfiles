#!/usr/bin/env bash


if [ $1 = "up" ]; then
  echo "Connecting..."
  expressvpn connect 'Canada - Vancouver'
elif [ $1 = "down" ]; then
  echo "Disconnecting..."
  expressvpn disconnect
elif [ $1 = "status" ]; then
  expressvpn status
else
  echo "Eh?"
fi
